{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ tag.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ tag.name }}</td>
        </tr>


        </tbody>
    </table>

    <h2>Liste des articles</h2>
    {% for article in articles %}
        <div>
        {{ article.title }}

        <a href="{{ path('blog_article',{'slug': article.url, 'id': article.id}) }}">Go</a>
    {% endfor %}
    </div>
<br>
    <div>
        <a href="{{ path('tag_index') }}">back to list</a>

        <a class="" href="{{ path('tag_edit', {'id': tag.id}) }}">edit</a>

        {{ include('tag/_delete_form.html.twig') }}

    </div>

{% endblock %}
